import './Keyboard.styles.css';

const Keyboard = () => {
  // const keyboardArray = [
  //   ['Q', 'W', 'E', 'R', 'T', 'Y', 'U', 'I', 'O', 'P', '<'],
  //   ['A', 'S', 'D', 'F', 'G', 'H', 'J', 'K', 'L', 'ENT'],
  //   ['Z', 'X', 'C', 'V', 'B', 'N', 'M']
  // ];

  // const handleClick = (letter, rowIndex, columnIndex) => {
  //   handleGuess(letter, rowIndex, columnIndex);
  // };

  // return (
  //   <div className='keyboard'>
  //     {keyboardArray.map((keyboardRow, rowIndex) => (
  //       <div key={rowIndex} className='keyboard-row'>
  //         {keyboardRow.map((letter, columnIndex) => (
  //           <button
  //             key={columnIndex}
  //             onClick={() => handleClick(letter, rowIndex, columnIndex)}
  //           >
  //             {letter}
  //           </button>
  //         ))}
  //       </div>
  //     ))}
  //   </div>
  // );
};

export default Keyboard;
